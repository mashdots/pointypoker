import styled from 'styled-components';

import InformationWrapper from './informationWrapper';

const DisconnectInfoWrapper = styled(InformationWrapper)`
  padding: 0;
`;

export default DisconnectInfoWrapper;
