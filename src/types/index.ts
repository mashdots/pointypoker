import type {
  Participant, PointOptions, Ticket, Room, Vote, RoomUpdateObject,
} from './legacy/room';
import type User from './user';

export {
  User,
  Participant,
  PointOptions,
  Ticket,
  Room,
  RoomUpdateObject,
  Vote,
};
