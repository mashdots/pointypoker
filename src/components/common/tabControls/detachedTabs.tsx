import React from 'react';
import styled, { css } from 'styled-components';

import { SubComponentProps } from '../tabControls';

const Wrapper = styled.div``;


const DetachedTabs = ({
  direction,
  display,
  onSetTab,
  selectedTab,
  stretch,
  tabs,
}: SubComponentProps) => {
  return <Wrapper></Wrapper>;
};

export default DetachedTabs;
